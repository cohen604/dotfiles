---
description: Run the checkpointed ticket-to-implementation workflow.
agent: lead
---

Coordinate delivery for `$ARGUMENTS`.

Perform ticket intake, repository scouting, necessary documentation research, and architecture. Present an implementation-ready plan and stop for approval before invoking `builder`. After implementation, run independent code and performance reviews, route blocking findings from either for correction, and run independent verification.

Do not mutate the tracker, commit, push, create a pull request, release, or deploy. At completion, propose exact next actions separately.
